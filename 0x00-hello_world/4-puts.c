#include <stdio.h>

/**
 * main - Entry point
 * This program print using puts.
 * It also returns 0  [returns(0)]
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}

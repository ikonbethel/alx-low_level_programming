#include "main.h"

/**
 * _isupper - This function checks for uppercase character.
 * @c: The variable return type is int
 * Return: This returns 1 for uppercase and  0 otherwise
 */
int _isupper(int c)
{
	if (c >=  'A' && c <= 'A')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
